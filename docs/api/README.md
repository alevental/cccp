# API Documentation

External interfaces — how users and tools interact with CCCP.

## Format

Each doc covers one interface surface. Required sections:

- **Commands/Tools** — with all parameters, types, defaults
- **Examples** — real invocations with expected output
- **Cross-links** — related architecture docs

Use tables for parameters. Show actual CLI output or MCP tool responses.
